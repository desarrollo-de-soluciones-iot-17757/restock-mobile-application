import 'package:flutter/material.dart';

class ProfileGeneralPage extends StatelessWidget {
  const ProfileGeneralPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
